// var palindrome = function(string) {
//   return ((string.split("")).join()) === ((string.split("").reverse()).join());
//
// };

var palindrome = function(string) {
  var a = string.split("");
  var b = string.split("").reverse();
    for (var i = 0; i >= a.length; i++);
      (a[i] == b[i]) {
      } else {
        return false;
      }
  }
};
